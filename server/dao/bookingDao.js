'use strict'

exports.deleteBooking = (bookingID) => {
    return new Promise ((resolve, reject) => {
        if(!bookingID){
            reject('Missing data');
        }

       // const sql = ``
    });
}